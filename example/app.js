const ExpressCore = require('../index');


const ip = 'localhost';
const port = 3000;
const apiRoot = '/'

const server = ExpressCore(ip, port);

// puedes añadir tus propios use
// app.use(morgan);


server.listen(this.#port, this.#ip, () => {

});

